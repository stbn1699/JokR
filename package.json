{
  "name": "jokr",
  "private": true,
  "version": "pre-beta 0.1",
  "description": "Jeux en ligne - JokR",
  "author": "Elina",
  "license": "AGPL-3.0+NonCommercial",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "update": "git fetch --all && git reset --hard origin/main && pnpm install -r --include=dev --frozen-lockfile",
    "build:web": "pnpm --filter web build",
    "build:api": "pnpm --filter api build",
    "build": "pnpm -r build",
    "deploy:web": "sudo mkdir -p /var/www/jokr.ebasson.fr && sudo rsync -a --delete apps/web/dist/ /var/www/jokr.ebasson.fr/",
    "start:api:prod": "pm2 delete jokr-api || true && pm2 start apps/api/dist/index.js --name jokr-api",
    "deploy:api": "pnpm build:api && pnpm start:api:prod",
    "updateDeploy:web": "pnpm update && pnpm build:web && pnpm deploy:web",
    "updateDeploy:api": "pnpm update && pnpm deploy:api",
    "updateDeploy": "pnpm update && pnpm build && pnpm deploy:web && pnpm start:api:prod",
    "init:web": "sudo mkdir -p /var/www/jokr.ebasson.fr",
    "init:pm2": "pm2 save"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "packageManager": "pnpm@10.18.3"
}
